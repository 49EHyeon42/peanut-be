spring:
  application:
    name: peanut
  datasource:
    url: jdbc:mysql://localhost:3306/peanutDatabase
    username: root
    password: '0000'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  output:
    ansi:
      enabled: always
